```
USAGE: boo [OPTIONS] <DB> [FILE]

ARGS:
    <DB>      path to places.sqlite db
    <FILE>    merge with previous export

OPTIONS:
    -h, --help             print help
    -i, --in-place         output to input export
    -o, --output <FILE>    output file
```
